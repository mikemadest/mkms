<!--section id="call-to-action">
  <div class="row">
    <div class="three columns header-col">
      <h1><img src="{{ page.media.images['preview.jpg'].url }}"><span>{{ page.header.title }}</span></h1>
    </div>
    <div class="six columns">
      {{ page.content }}
    </div>
    <div class="three columns action">
      {% for button in page.header.buttons %}
      <a href="{{ button.url }}" class="button" target="_blank">{{ button.text }}</a>
      {% endfor %}
    </div>
  </div>
</section-->
